# Optional. Leave unset and the app runs entirely on the on-device matcher.
# When set, a "Read this more closely" button appears after a search and posts
# to this endpoint. Never put an API key here — anything in the app binary can
# be extracted. See "Turning on the closer reading" in README.md.
EXPO_PUBLIC_READING_ENDPOINT=
